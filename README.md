# 📚 BookishStore

A full-stack e-commerce web application for an online bookshop, built with **React + Vite** on the frontend and **Node.js/Express** on the backend.

---

## 🛠️ Tech Stack

| Layer | Technology |
|-------|-----------|
| Frontend | React, Vite, CSS |
| Backend | Node.js, Express |
| Language | JavaScript |

---

## 📁 Project Structure

```
BookishStore/
├── frontend/        # React + Vite app
│   ├── src/
│   └── ...
├── backend/         # Node.js/Express API
│   ├── ...
└── README.md
```

---

## 🚀 Getting Started

### Prerequisites

- Node.js (v18+)
- npm

### Installation

Clone the repository:

```bash
git clone https://github.com/camescopetech/BookishStore.git
cd BookishStore
```

Install dependencies for both frontend and backend:

```bash
cd backend && npm install
cd ../frontend && npm install
```

### Running the app

Start the backend server:

```bash
cd backend
npm start
```

Start the frontend development server:

```bash
cd frontend
npm run dev
```

The frontend will be available at `http://localhost:5173` and the backend at `http://localhost:3000` (or whichever port is configured).

---

## ✨ Features

- Browse and view a catalog of books
- Book detail pages with descriptions and pricing
- Shopping cart with back navigation
- Contact form
- Checkout / payment page

---

## 👥 Authors

Bezet Camille — CY Tech ING3 IA A
